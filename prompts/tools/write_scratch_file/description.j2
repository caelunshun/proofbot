Creates a scratch Lean file and compiles it, returning
the diagnostics. The file is placed in the project
root, so it can import other project modules. 
Use this tool to test out partial solutions. The scratch file is deleted
immediately after being compiled, so you must re-write it if you want to
make changes. Do not try to write code with side-effects.